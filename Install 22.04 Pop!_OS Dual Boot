#  Get ready
  Phone to approve google requests and usb tethering if needed
  Make space for linux - disk management utility in windows. Shrink by 350000 on 1 TB drive.  Shrink windows partition.  Note partition #. (8).  Note any others you want to take over (5)
  Have >4 GB thumbdrive
  On Windows download iso.  flash using balenaEtcher
  Turn off bitlocker all drives
  Deauthorize tools like DVDfab, winxdvd, epubor in case can't get back
  Unplug second monitor, other usb's.  May leave mouse and keypad
  Create windows restore point
  Save github on windows

# insert iso usb
  F1 (rapid F12 on Lenovo laptop, F10 on HP
  Omen, F7 on little_guy) during boot.   Set boot order to USB.  *try or install ubuntu - normal installation - install updates (I tried minimal installation - lots of things didn't work right)
  Choose 'try or install Ubuntu.'  3 min to next step - will hear music
  Go through menus ~5 min
    Network - for next step
    Update to new version (green button)
    Full installation
    Custom:
      #p5 ext4 / all
      p5 swap        35 GB (larger than 32 GB Ram)
      p6 ext4 /      21 GB (recommended by web link)
      p7 ext4 /boot   2 GB (") large to hold both windows and pop
      p8 ext4 /home  balance GB
    daveg
  Wait ~12 min for install
    When prompted to reboot leave stick and wait until prompted to remove it
    I start with one letter password until end then change using instructions herein 
    
  If this won't 'install' (menu item grayed out, verify Fat32 and other stuff.  Then simply restart with the stick still in and try again.

# Pop!_Shop installs
  brasero
  caffeine (the one with steam rising)
  vlc (open and turn off hardware accel)
  sublime text
  NoMachine
  GitHub desktop
  Synaptic

# Flatpak used in pop!os makes path errors for pycharm-community (won't find special python build in /usr/local/bin).  Have to install it using snap then do some extra setup steps.
  # uninstall the pycharm-community already installed
  sudo apt install snapd
  sudo snap install --classic pycharm-community

sudo kernelstub -a mem_sleep_default=deep  # prevent not waking after suspend
  Restart

# Zoom desktop
  https://zoom.us/download?os=linux  # download Ubuntu 64 bit
  sudo gdebi ./Downloads/zoom_amd64.deb
  Launch from Apps and signin with Google

# Guake
  sudo apt install guake
  # set to start on login
  
# Ferdiun
  Install ferdium using File - System Discover
  
# Rclone + Drive
  see Ubuntu

# glances system monitor
  sudo apt install glances

# Firefox busy save setting
about:config
browser.sessionstore.interval 150000

# Python project notes (movie_Scraper, SOC, etc):   see 'Install 24.04 Ubuntu.txt'


# Install apps
  sudo snap install snapd
  logout / log back in to organize snapd ########## important!!!!  IT WILL WORK IF YOU DO THIS
  #sudo snap install plexmediaserver # for remote laptop (for trips) as well as basement server
  #sudo apt install ./Downloads/code_1.90.2-1718751586_amd64.deb
  sudo apt install -y fuse libfuse2  # For appImage
  sudo apt install -y ffmpeg
  sudo apt install -y git
  sudo apt install --fix-missing -y python3-pip
  sudo apt install -y python3-tk  # for pyCharm
  sudo apt install -y dhcpcd5
  sudo apt install dos2unix
#  sudo apt install LocalSend
  sudo apt install xsel
  sudo apt install -y pavucontrol  # for myPyScreencast
  

# Another python version
  see Ubuntu

  # Configure sound
  sudo nano /etc/modules-load.d/modules.conf
  # Add following line to /etc/modules-load.d/modules.conf
  snd-aloop
  # To start it for this session
  sudo modprobe snd-aloop
  # you can just plug in the usb headset and adjust the sound settings

# Firefox busy save setting
  about:config
  browser.sessionstore.interval 150000

   # Speed up network
    see Ubuntu

# firewall
      sudo apt install gufw




#########################works in progress  ######################

#  try to make boot menu work


# check this in Microsoft first
  you can confirm your Windows system is using UEFI mode by opening the System Information app and hecking the value of "BIOS Mode," which will be "UEFI" if in UEFI mode. Alternatively, you can se Disk Management to look for an "EFI System Partition" or in Command Prompt with the command reg uery HKLM\System\CurrentControlSet\Control /v PEFirmwareType to see if the DWORD value is 

  fast start disabled (Control Panel - Power)   CHECK!

  Bitlocker disabled  CHECK!

sing System Information (Recommended Method)
This is the most straightforward and common method to check your BIOS mode. 

    Open System Information: Press the Windows key + R to open the Run dialog box.
    Type msinfo32: In the Run box, type msinfo32 and press Enter.
    Locate BIOS Mode: In the System Information window, find the "System Summary" section.
    Check the Value: Look for the entry labeled "BIOS Mode". If it says "UEFI," your system is running in UEFI mode.   CHECK!


  sudo apt install os-prober
  sudo os-prober
  # grab the output:  /dev/nvme0n1p1@/efi/Microsoft/Boot/bootmgfw.efi
  sudo mount /dev/nvme0n1p1 /mnt
  sudo su
  cp -R /mnt/efi/Microsoft /boot/efi/Microsoft
  ctrl-d to exit
  
  sudo nano /boot/efi/loader/loader.conf
  add 'timeout 8' to this file
  

 # create conf in entries folder for each os

 #  this was made automatically during install:  Pop_OS-current.conf
 root@pop-os:/boot/efi/loader/entries# more Pop_OS-current.conf 

title Pop!_OS
linux /EFI/Pop_OS-c6774fc5-6e26-4eee-a040-36c5f2bcb792/vmlinuz.efi
initrd /EFI/Pop_OS-c6774fc5-6e26-4eee-a040-36c5f2bcb792/initrd.img
options root=UUID=c6774fc5-6e26-4eee-a040-36c5f2bcb792 ro quiet loglevel=0 systemd.show_status=false splash nvidia-drm.modeset=1 mem_sleep_default=deep

 # example from web for Windows: windows.conf  assumes standard location of windows boot manager (efi partition)
title Windows
efi /EFI/Microsoft/Boot/bootmgfw.efi

Windows partition info
/dev/nvme0n1p4 uuid 34B0CE20B0CDE906  NTFS 600 G B
/dev/nvme0n1p1 1GB D18D-4F8F
  reboot.  In Bios change order so Pop-OS is at the top.  Save & exit & continue to boot
  


To enable the boot menu in a Pop!_OS dual-boot setup, ensure the
os-prober utility is installed and then run sudo update-grub to detect other operating systems like Windows. Then, modify your EFI bootloader's configuration to set a timeout for the menu to appear before booting into the default OS. You can access the boot menu during startup by pressing a designated key (often F12 or ESC) to choose your operating system. 

(https://www.google.com/search?q=dual+boot+turn+on+grub+popos&client=firefox-b-1-d&sca_esv=a07cb0739b81081f&sxsrf=AE3TifNms6cYYMmseqKs5L_VNjY4jcrqqQ%3A1755691633780&ei=cbqlaMWzL_zWptQPio2i2Qk&ved=0ahUKEwiFsKqBrZmPAxV8q4kEHYqGKJsQ4dUDCBA&uact=5&oq=dual+boot+turn+on+grub+popos&gs_lp=&sclient=gws-wiz-serp)

